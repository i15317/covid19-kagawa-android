<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="item"
            type="jp.covid19_kagawa.covid19information.store.GuideStore" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/root_text"
            android:layout_width="match_parent"
            android:layout_height="32dp"
            android:autoSizeTextType="uniform"
            android:maxLines="1"
            android:padding="4dp"
            android:text="@{item.titleMessage}"
            android:textStyle="bold"
            app:layout_constraintBottom_toTopOf="@id/scene_root"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <FrameLayout
            android:id="@+id/scene_root"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/root_text">

        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
